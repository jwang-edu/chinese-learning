{
  "name": "hanzi-practice-companion",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/copy-static-assets.mjs",
    "build:pinyin-audio": "node scripts/download-pinyin-audio.mjs",
    "build:word-db": "node scripts/build-word-db-from-cedict.mjs node_modules/cedict data/word-db.js",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "dependencies": {
    "cedict": "^1.45.0"
  }
}
